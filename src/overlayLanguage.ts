// Language identity used to isolate visible shell source from direct Python providers.

/** Language ID whose syntax mirrors Python while language features route through the complete hidden analysis document. */
export const OVERLAY_SHELL_LANGUAGE_ID = "django-shell-python";
