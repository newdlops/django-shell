// Shares byte limits for backend socket frames before JSON parsing or network submission.
export const BACKEND_REQUEST_BYTES = 4 * 1024 * 1024;
export const BACKEND_RESPONSE_BYTES = 16 * 1024 * 1024;
